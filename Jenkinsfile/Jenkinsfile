@Library('mylib1') _

node{
    stage('Build'){
        checkout scm
        echo 'start build'
    }
    stage('Test'){
        checkout scm
        echo 'start test'
        res=readFile('tclist.txt')
        echo res
    }
    stage('Deploy'){
        checkout scm
        echo 'start deploy'
    }
}